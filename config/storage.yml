test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

production_disk:
  service: Disk
  root: <%= Rails.root.join("public/storage") %>
